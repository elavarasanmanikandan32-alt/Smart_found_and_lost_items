const { User, userSchema } = require('./User');
const { Item, itemSchema } = require('./Item');
const { Claim, claimSchema } = require('./Claim');

module.exports = {
  User,
  userSchema,
  Item,
  itemSchema,
  Claim,
  claimSchema,
};
