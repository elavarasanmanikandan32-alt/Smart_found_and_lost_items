const { userSchema, User } = require('./userSchema');
const { itemSchema, Item } = require('./itemSchema');
const { claimSchema, Claim } = require('./claimSchema');

module.exports = {
  userSchema,
  User,
  itemSchema,
  Item,
  claimSchema,
  Claim,
};
